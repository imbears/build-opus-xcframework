# build-opus-xcframework

### 将build-opus-xcframework.sh文件下载到本地，执行：

```shell
chmod +x build-opus-xcframework.sh
./build-opus-xcframework.sh
# or 
sh ./build-opus-xcframework.sh
```

### 修改版本：

可修改SOURCE_VERSION="v1.6.1"至最新版本

### 最后：

本脚本是通过deepseek生成且验证通过

